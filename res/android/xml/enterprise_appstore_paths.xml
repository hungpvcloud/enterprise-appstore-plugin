<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- App-specific external files directory (No n PERMISSION) -->
    <external-files-path
        name="enterprise_apk_downloads"
        path="Downloads/" />

    <!-- Fallback: Cache directory -->
    <cache-path
        name="enterprise_apk_cache"
        path="apk_cache/" />

    <!-- (Must have MANAGE_EXTERNAL_STORAGE on Android 11+) -->
    <!-- <external-path
        name="enterprise_apk_public_downloads"
        path="Download/" /> -->
    <!-- Cache directory -->
    <cache-path name="cache" path="." />

</paths>
